import { IOptions } from './../types';

export const defaultOptions: IOptions = {
  implicitFlow: true,
  bannerTemplate: 'default',
  preferenceCenterTemplate: 'classic',
  container: 'body',
  overrideLangauge: 'en',
  overrideLocation: 'default',
};
